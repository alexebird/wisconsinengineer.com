require 'spec_helper'

describe Article do
  #before(:each) do
    #load 'db/seeds'
    #@user = User.find :first
    #@issue = Issue.create(:published => Time.now, :user => @user)
    #@category = Category.create(:name => "article", :user => @user)
    #@valid_attributes = {
      #:headline => "Bricks builds house",
      #:lead => "House is amazing",
      #:contents => "This is the article."
    #}
  #end

  #it "should create a new instance given valid attributes" do
    #a = Article.new(@valid_attributes)
    #a.user = @user
    #a.category = @category
    #a.issue = @issue
    #s.save
  #end
end
